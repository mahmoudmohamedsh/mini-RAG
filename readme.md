```bash
# how use linux terminal in windows by using Windows Subsystem for Linux (WSL) package
#  كده بيئه العمل بتكون لينكس
wsl --install
wsl --set-default-version 2
wsl --version
wsl --install Ubuntu

sudo apt update 

cd /mnt/<Partision>/dir
ex : /mnt/C/user
```

# mini-RAG

This is a miniaml implementation of the RAG model for qustions answering.

## Requirements

- python 3.8 or later


python boilerplate github